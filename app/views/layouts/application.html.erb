<!DOCTYPE html>
<html>
  <head>
    <title>Td7Flats</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if property_owner_signed_in? %>
        <%= link_to current_property_owner.email, my_profile_path %>
        <%= link_to 'Meus imóveis', my_properties_properties_path %>
        <%= link_to 'Logout', destroy_property_owner_session_path, method: :delete %>
        <%= link_to "Cadastrar imóvel", new_property_path, :id => 'register_link' %>
      <% elsif user_signed_in? %>
        <%= link_to 'Meu perfil', my_profile_path %>s
      <% else %>
        <%= link_to 'Entrar', new_property_owner_session_path %>
      <% end %>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
